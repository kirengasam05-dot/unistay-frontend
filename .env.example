# Base URL for the UniStay+ backend API (note the /api suffix)
VITE_API_BASE_URL=https://cdn-unistay.onrender.com/api
# For a locally running backend, use:
# VITE_API_BASE_URL=http://localhost:3000/api
