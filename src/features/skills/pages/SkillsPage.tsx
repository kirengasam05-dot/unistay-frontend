import { courses, assignments, certificates } from '../../../data/mockData';
import Badge from '../../../components/ui/Badge';
import Button from '../../../components/ui/Button';
export default function SkillsPage(){ return <section className="mx-auto max-w-7xl px-6 py-16"><h1 className="text-5xl font-black">Skills, courses, exams and certificates</h1><p className="mt-3 text-neutral-600">Study employer-uploaded courses, complete materials, take exams, and earn certificates for your profile.</p><div className="mt-8 grid gap-6 md:grid-cols-3">{courses.map(c=><div className="card p-3" key={c.id}><img className="h-44 w-full rounded-2xl object-cover" src={c.thumbnail}/><div className="p-4"><Badge>{c.category}</Badge><h2 className="mt-3 text-xl font-black">{c.title}</h2><p className="text-sm text-neutral-600">{c.description}</p><div className="mt-3 h-2 rounded-full bg-neutral-100"><div className="h-2 rounded-full bg-black" style={{width:`${c.progress}%`}}/></div><Button className="mt-4 w-full">Continue learning</Button></div></div>)}</div><div className="mt-12 grid gap-6 lg:grid-cols-2"><div className="card"><h2 className="text-2xl font-black">Available Exams</h2>{assignments.map(a=><div className="mt-3 rounded-2xl border p-4" key={a.id}><b>{a.title}</b><p className="text-sm text-neutral-600">{a.questionCount} questions • Passing {a.passingScore}%</p></div>)}</div><div className="card"><h2 className="text-2xl font-black">Certificates</h2>{certificates.map(c=><div className="mt-3 rounded-2xl border p-4" key={c.id}><b>Certificate #{c.id}</b><p className="text-sm text-neutral-600">Issued {c.issuedAt}</p></div>)}</div></div></section> }
