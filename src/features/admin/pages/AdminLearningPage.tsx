import { useState } from 'react';
import Input from '../../../components/ui/Input';
import Button from '../../../components/ui/Button';
import { courses as initial } from '../../../data/mockData';
export default function AdminLearningPage(){ const [courses,setCourses]=useState(initial); const [title,setTitle]=useState(''); const add=()=>{ if(!title.trim()) return; setCourses([{id:crypto.randomUUID(),title,description:'New course description',category:'Digital Skills',progress:0,isPublished:false,thumbnail:'https://images.unsplash.com/photo-1516321318423-f06f85e504b3?auto=format&fit=crop&w=900&q=80',materials:1,exam:'New Exam',certificateAvailable:false},...courses]); setTitle(''); }; return <div className="space-y-6"><div className="card"><h1 className="text-3xl font-black">Course, Video, Exam & Assignment Builder</h1><p className="mt-2 text-neutral-600">Admin controls learning content: create courses, upload video/PDF/article materials, set assignments, exams, and passing scores.</p><div className="mt-5 grid gap-3 md:grid-cols-[1fr_auto]"><Input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Course title"/><Button onClick={add}>Create course</Button></div></div>{courses.map(c=><div className="card" key={c.id}><div className="flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-xl font-black">{c.title}</h2><p className="text-sm text-neutral-500">{c.category} · {c.materials} materials · Exam: {c.exam}</p></div><div className="flex flex-wrap gap-2"><button className="rounded-2xl bg-black px-4 py-2 font-bold text-white">Upload video</button><button className="rounded-2xl bg-black px-4 py-2 font-bold text-white">Set exam</button><button className="rounded-2xl bg-black px-4 py-2 font-bold text-white">Add assignment</button><button onClick={()=>setCourses(courses.map(x=>x.id===c.id?{...x,isPublished:!x.isPublished}:x))} className={`rounded-2xl px-4 py-2 font-bold text-white ${c.isPublished?'bg-green-600':'bg-neutral-700'}`}>{c.isPublished?'Published':'Publish'}</button></div></div></div>)}</div> }
