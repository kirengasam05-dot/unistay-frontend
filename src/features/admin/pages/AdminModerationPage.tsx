import { useState } from 'react';
const pending=[{id:'m1',type:'Housing',title:'Kimironko Private Studio',status:'PENDING'},{id:'m2',type:'Job',title:'Campus Sales Assistant',status:'PENDING'}];
export default function AdminModerationPage(){ const [items,setItems]=useState(pending); return <div className="space-y-6"><div className="card"><h1 className="text-3xl font-black">Housing & Job Moderation</h1><p className="mt-2 text-neutral-600">Approve verified listings and reject unsafe or incomplete content.</p></div>{items.map(i=><div className="card" key={i.id}><div className="flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-xl font-black">{i.title}</h2><p className="text-sm text-neutral-500">Type: {i.type}</p></div><div className="flex gap-2"><button onClick={()=>setItems(items.map(x=>x.id===i.id?{...x,status:'VERIFIED'}:x))} className="rounded-2xl bg-green-600 px-4 py-2 font-bold text-white">Approve</button><button onClick={()=>setItems(items.map(x=>x.id===i.id?{...x,status:'REJECTED'}:x))} className="rounded-2xl bg-red-600 px-4 py-2 font-bold text-white">Reject</button></div></div><p className="mt-4 text-sm font-black">Status: {i.status}</p></div>)}</div> }
