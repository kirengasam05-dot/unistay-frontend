import { Link } from 'react-router-dom';
import { BookOpen, Briefcase, Building2, CheckCircle2 } from 'lucide-react';
import { courses, housings, jobs } from '../../data/mockData';
const Stat=({label,value,icon:Icon}:{label:string;value:string;icon:any})=><div className="card"><div className="flex items-center justify-between"><div><p className="text-sm font-semibold text-neutral-500">{label}</p><h3 className="mt-2 text-3xl font-black">{value}</h3></div><div className="grid h-12 w-12 place-items-center rounded-2xl bg-black text-white"><Icon size={20}/></div></div></div>
export default function StudentDashboard(){ return <div className="space-y-6"><div className="rounded-[2rem] bg-black p-8 text-white"><p className="font-bold text-neutral-300">Student workspace</p><h1 className="mt-2 text-4xl font-black">Book housing, apply for jobs, learn skills, and earn certificates.</h1><p className="mt-3 max-w-3xl text-neutral-300">Payment is allowed only after host confirms your booking. Job applications are reviewed by employers based on skill compatibility.</p><div className="mt-6 flex flex-wrap gap-3"><Link className="btn-white" to="/student/booking">Start housing booking</Link><Link className="rounded-2xl border border-white/30 px-5 py-3 font-bold text-white" to="/student/learning">Continue learning</Link></div></div><div className="grid gap-4 md:grid-cols-4"><Stat label="Available housing" value={String(housings.filter(h=>h.availability).length)} icon={Building2}/><Stat label="Matched jobs" value={String(jobs.length)} icon={Briefcase}/><Stat label="Course progress" value="80%" icon={BookOpen}/><Stat label="Certificates" value="1" icon={CheckCircle2}/></div><div className="grid gap-6 lg:grid-cols-2"><div className="card"><h2 className="text-xl font-black">Booking process</h2><div className="mt-5 space-y-4">{['Choose verified housing','Send booking request','Wait for host confirmation','Pay only after confirmed','Move in safely'].map((s,i)=><div className="flex gap-3" key={s}><span className="grid h-8 w-8 shrink-0 place-items-center rounded-full bg-black text-sm font-bold text-white">{i+1}</span><p className="font-semibold">{s}</p></div>)}</div></div><div className="card"><h2 className="text-xl font-black">Learning path</h2>{courses.slice(0,2).map(c=><div key={c.id} className="mt-5 rounded-3xl border border-neutral-200 p-4"><div className="flex items-center justify-between"><p className="font-black">{c.title}</p><span className="text-sm font-bold">{c.progress}%</span></div><div className="mt-3 h-2 rounded-full bg-neutral-100"><div className="h-2 rounded-full bg-green-600" style={{width:`${c.progress}%`}}/></div></div>)}</div></div></div> }
