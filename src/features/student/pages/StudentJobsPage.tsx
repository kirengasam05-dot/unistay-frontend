import { useState } from 'react';
import { jobs } from '../../../data/mockData';
export default function StudentJobsPage(){ const [applied,setApplied]=useState<string[]>([]); return <div className="space-y-6"><div className="card"><h1 className="text-3xl font-black">Search Jobs</h1><p className="mt-2 text-neutral-600">Apply to jobs that match your skills. Employer will review compatibility and send result by email.</p><input className="mt-5 w-full rounded-2xl border border-neutral-300 px-4 py-3 outline-none focus:border-black" placeholder="Search by title, location, schedule or skill"/></div><div className="grid gap-4">{jobs.map(j=><div className="card" key={j.id}><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="text-xl font-black">{j.title}</h2><p className="mt-1 text-sm text-neutral-500">{j.location} · {j.scheduleType} · RWF {j.salary.toLocaleString()}</p><div className="mt-3 flex flex-wrap gap-2">{j.requiredSkills.map(s=><span key={s} className="rounded-full bg-neutral-100 px-3 py-1 text-xs font-bold">{s}</span>)}</div></div>{applied.includes(j.id)?<button className="rounded-2xl bg-green-600 px-5 py-3 font-bold text-white">Applied — email pending</button>:<button onClick={()=>setApplied([...applied,j.id])} className="rounded-2xl bg-black px-5 py-3 font-bold text-white">Apply now</button>}</div></div>)}</div></div> }
