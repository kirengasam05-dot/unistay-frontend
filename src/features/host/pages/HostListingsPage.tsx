import { useState } from 'react';
import Input from '../../../components/ui/Input';
import Button from '../../../components/ui/Button';
import { housings as initial } from '../../../data/mockData';
export default function HostListingsPage(){ const [items,setItems]=useState(initial); const [title,setTitle]=useState(''); const add=()=>{ if(!title.trim()) return; setItems([{id:crypto.randomUUID(),title,location:'Kigali',price:150000,availability:true,verificationStatus:'PENDING',hostId:'u-host',image:'https://images.unsplash.com/photo-1560448204-e02f11c3d0e2?auto=format&fit=crop&w=900&q=80'},...items]); setTitle(''); }; return <div className="space-y-6"><div className="card"><h1 className="text-3xl font-black">Add Housing</h1><p className="mt-2 text-neutral-600">Create housing listings and keep availability accurate before student payment.</p><div className="mt-5 grid gap-3 md:grid-cols-[1fr_auto]"><Input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Housing title"/><Button onClick={add}>Add listing</Button></div></div><div className="grid gap-4">{items.map(h=><div className="card" key={h.id}><div className="flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-xl font-black">{h.title}</h2><p className="text-sm text-neutral-500">{h.location} · RWF {h.price.toLocaleString()} · {h.verificationStatus}</p></div><div className="flex gap-2"><button onClick={()=>setItems(items.map(x=>x.id===h.id?{...x,availability:!x.availability}:x))} className={`rounded-2xl px-4 py-2 font-bold text-white ${h.availability?'bg-green-600':'bg-red-600'}`}>{h.availability?'Available':'Booked'}</button><button onClick={()=>setItems(items.filter(x=>x.id!==h.id))} className="rounded-2xl bg-red-600 px-4 py-2 font-bold text-white">Delete</button></div></div></div>)}</div></div> }
