import { useState } from 'react';
const requests=[{id:'b1',student:'Aline Student',housing:'Kacyiru Student Residence',dates:'10 Jun - 10 Aug',status:'PENDING'},{id:'b2',student:'Claude Student',housing:'Remera Shared Apartment',dates:'15 Jun - 15 Sep',status:'PENDING'}];
export default function HostBookingsPage(){ const [bookings,setBookings]=useState(requests); return <div className="space-y-6"><div className="card"><h1 className="text-3xl font-black">Bookings & Availability Confirmation</h1><p className="mt-2 text-neutral-600">Confirm only if housing is available. Student payment unlocks after confirmation.</p></div>{bookings.map(b=><div className="card" key={b.id}><div className="flex flex-wrap items-center justify-between gap-4"><div><h2 className="text-xl font-black">{b.housing}</h2><p className="text-sm text-neutral-500">Student: {b.student} · {b.dates}</p></div><div className="flex gap-2"><button onClick={()=>setBookings(bookings.map(x=>x.id===b.id?{...x,status:'CONFIRMED'}:x))} className="rounded-2xl bg-green-600 px-4 py-2 font-bold text-white">Confirm available</button><button onClick={()=>setBookings(bookings.map(x=>x.id===b.id?{...x,status:'CANCELLED'}:x))} className="rounded-2xl bg-red-600 px-4 py-2 font-bold text-white">Reject</button></div></div><p className="mt-4 text-sm font-bold">Current status: {b.status}</p></div>)}</div> }
